#ifndef _MOVE_H_
#define _MOVE_H

#include <stdio.h>

move_is_valid();
move_check();
move_pawn(piece* p);
move_rook(piece* p);
move_knight(piece* p);
move_bishop(piece* p);
move_king(piece* p);
move_queen(piece* p);

#endif
